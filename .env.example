# ⚠️  SECURITY WARNING: This is a template file!
# Copy this file to .env and replace ALL placeholder values with real secrets
# NEVER commit real credentials to version control!

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# Option 1: Use Neon connection string (recommended)
DATABASE_URL=postgresql://username:password@hostname:5432/database_name?sslmode=require

# Option 2: Individual database parameters (fallback)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=qr_checkin
DB_USER=postgres
DB_PASSWORD=password
DB_SSL=false

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=24h

# QR Code Configuration
QR_ENCRYPTION_KEY=your-32-character-encryption-key-change-this-in-production
QR_EXPIRATION_SECONDS=60

# File Upload Configuration
UPLOAD_MAX_SIZE=5242880
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/webp

# CORS Configuration
CORS_ORIGIN=http://localhost:3000